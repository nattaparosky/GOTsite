import './Banner.css'

export const Banner = () => {
    //JSX - é como o React trabalha com a parte visual
    return (
        <header className="banner">
    <img src="/images/banner.png" 
         alt="the main top banner"/>
        </header>
    )
}